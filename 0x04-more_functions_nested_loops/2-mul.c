#include "holberton.h"
#include <ctype.h>
/**
 *mul - multiplies the value of two argument
 *@a: parameter for mul
 *@b: parameter fro mul
 *Return: 1 or 0
 */
int mul(int a, int  b)
{
int r;
r = (a * b);
return (r);
}
